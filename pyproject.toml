[project]
name = "juzzy-yandexgpt"
version = "0.0.1"
description = "YandexGPT model provider plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.5.0,<0.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.2",
]
